import sys


#n = int(raw_input().strip())
#arr = map(int,raw_input().strip().split(' '))

xs = raw_input('give one number: ')
n=int(xs.strip())


ll= raw_input('give a sequence:')
arr = (map(int,ll.strip().split(' ')))



rev =[]
for i in range(n):

    val=arr[n-i-1]
    rev.append(val)


revv= ' '.join((map(str,rev)))
print ( revv)


